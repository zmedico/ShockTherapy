<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.googlecode.electroshocktherapy"
    android:versionCode="2"
    android:versionName="1.0.0.1" >

    <!--
    Set targetSdkVersion less than 14 so that the system navigation bar
    shows an overflow button that triggers the menu, as explained here:
    http://android-developers.blogspot.com/2012/01/say-goodbye-to-menu-button.html
    -->
    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="13" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyTheme" >
        <activity
            android:name="com.googlecode.electroshocktherapy.ShockTherapyActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
